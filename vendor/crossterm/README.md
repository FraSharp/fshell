# Crossterm (fshell downstream source)

This is crossterm 0.29.0, retained as a small downstream fork until an upstream
release includes the Unix terminal EOF fixes described in `FSHELL.md`.

Upstream project: https://github.com/crossterm-rs/crossterm

License: MIT; see `LICENSE`.
